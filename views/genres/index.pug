extends ../layout

block content
  h1.page-header Genres
  for genre in genres
    .row.jumbotron
      .col-md-4
        img.media-object.thumbnail(src= genre.image_url, alt='...')
      .col-md-8
        a(href="/genres/"+genre.id)
          h3= genre.name
        p= genre.description
