extends ../layout

block content
  h1 All Authors
  for author in authors
    .row
      .col-md-4
        img.media-object(src= author.image_url, alt='...')
        a(href="/authors/"+author.id)= author.name
      .col-md-8
        p= author.description
